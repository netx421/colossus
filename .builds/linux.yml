image: archlinux

packages:
  - pkg-config
  - cmake
  - make
  - freetype2
  - fontconfig
  - libxcb
  - libxkbcommon
  - scdoc

sources:
  - https://github.com/colossus/colossus

environment:
  PATH: /home/build/.cargo/bin:/usr/bin/

tasks:
  - rustup: |
      curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal
  - rustfmt: |
      cd colossus
      rustup toolchain install nightly -c rustfmt
      cargo +nightly fmt -- --check
  - man-pages: |
      cd colossus
      cat extra/man/colossus.1.scd | scdoc > /dev/null
      cat extra/man/colossus-msg.1.scd | scdoc > /dev/null
      cat extra/man/colossus.5.scd | scdoc > /dev/null
      cat extra/man/colossus-bindings.5.scd | scdoc > /dev/null
  - test: |
      cd colossus
      cargo test
  - oldstable: |
      cd colossus
      oldstable=$(cat Cargo.toml | grep "rust-version" | sed 's/.*"\(.*\)".*/\1/')
      rustup toolchain install --profile minimal $oldstable
      rustup default $oldstable
      cargo test
  - clippy: |
      cd colossus
      rustup component add clippy
      cargo clippy --all-targets
  - feature-wayland: |
      cd colossus/colossus
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=wayland
  - feature-x11: |
      cd colossus/colossus
      RUSTFLAGS="-D warnings" cargo test --no-default-features --features=x11
